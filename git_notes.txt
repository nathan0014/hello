push changes onto the server
pull other peoples changed onto your computer 
branch to test the code then merge to add those changes to the real thing
"git clone" gets stuff from git onto computer
commit is when you want to save the state of a file 
"git add" adds a file that will be tracked by git 
"git commit -m "message"" message of what you have changed
"git status" tells us whats on my computer vs on github
"git push

terminal 
"ls" lists all files in directory
"touch" creates a file on LINUX
"New-Item -ItemType File" then name of the file 
